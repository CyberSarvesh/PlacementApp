//import express from 'express';
//import { getUsersByEligibility } from '../controllers/userFilterGrade.js';

//const router = express.Router();

// Route to fetch users based on eligibility criteria
//router.get('/:min_cgpa/:min_highschool_score', getUsersByEligibility);

//export default router;
